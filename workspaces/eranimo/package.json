{
  "name": "eranimo",
  "version": "0.0.0",
  "description": "Create a file in your directory if it's empty to allow you to push the directory. Git won't allow you to push an empty folder.",
  "main": "one.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mime": "~1.2.11"
  }
}
